# Navigation Flow Diagram

## Before Fix (❌ Problematic)

```
┌─────────────────────────────────────────────────────────────┐
│                     BEFORE FIX                               │
└─────────────────────────────────────────────────────────────┘

User Journey:
┌──────┐    navigate    ┌──────────────┐    save    ┌──────────────┐
│ Home │ ──────────────→│   Selection  │ ─────────→ │   Selection  │
└──────┘                └──────────────┘            └──────────────┘
                        (selecting meals)            (saved state)
                                                            │
                                                            │ back button
                                                            ↓
                        ┌──────────────┐                   
                        │   Selection  │ ←─────────────────┘
                        └──────────────┘
                        (OLD STATE VISIBLE! ❌)


History Stack:
┌─────────────────────────────────────────┐
│  4. Selection (saved)                   │ ← Current
├─────────────────────────────────────────┤
│  3. Selection (selecting)               │
├─────────────────────────────────────────┤
│  2. Selection (initial)                 │
├─────────────────────────────────────────┤
│  1. Home                                │
└─────────────────────────────────────────┘

Problem: Back button goes through all states!
```

---

## After Fix (✅ Correct)

```
┌─────────────────────────────────────────────────────────────┐
│                     AFTER FIX                                │
└─────────────────────────────────────────────────────────────┘

User Journey:
┌──────┐    navigate    ┌──────────────┐    save    ┌──────┐
│ Home │ ──────────────→│   Selection  │ ─────────→ │ Home │
└──────┘   (replace)    └──────────────┘  (redirect) └──────┘
                        (selecting meals)   (1.5s)        │
                                                           │
                                                           │ back button
                                                           ↓
                                                    ┌─────────────┐
                                                    │  Exit App   │
                                                    └─────────────┘


History Stack:
┌─────────────────────────────────────────┐
│  1. Home (after redirect)               │ ← Current
└─────────────────────────────────────────┘

Clean! Only one entry in history.
```

---

## Navigation Guard Mechanism

```
┌─────────────────────────────────────────────────────────────┐
│              HOW NAVIGATION GUARD WORKS                      │
└─────────────────────────────────────────────────────────────┘

Step 1: Component Mounts
┌──────────────────────────────────────┐
│  MealSelection.jsx loads             │
│  ↓                                   │
│  useEffect runs                      │
│  ↓                                   │
│  window.history.replaceState()       │
│  (replaces current entry)            │
│  ↓                                   │
│  addEventListener('popstate')        │
│  (listens for back button)           │
└──────────────────────────────────────┘

Step 2: User Presses Back Button
┌──────────────────────────────────────┐
│  User clicks back button             │
│  ↓                                   │
│  'popstate' event fires              │
│  ↓                                   │
│  handlePopState() called             │
│  ↓                                   │
│  e.preventDefault()                  │
│  (stops default back)                │
│  ↓                                   │
│  navigate('/', { replace: true })    │
│  (redirect to home)                  │
└──────────────────────────────────────┘

Step 3: Cleanup
┌──────────────────────────────────────┐
│  Component unmounts                  │
│  ↓                                   │
│  useEffect cleanup runs              │
│  ↓                                   │
│  removeEventListener('popstate')     │
│  (prevent memory leaks)              │
└──────────────────────────────────────┘
```

---

## Auto-Redirect Flow

```
┌─────────────────────────────────────────────────────────────┐
│              SAVE/SUBMIT AUTO-REDIRECT                       │
└─────────────────────────────────────────────────────────────┘

Timeline:
0ms                    1000ms                 1500ms
│                        │                      │
│  User clicks Save      │  Toast visible       │  Redirect
│  ↓                     │  ↓                   │  ↓
│  toast.success()       │  "Saved!"            │  navigate('/')
│  setTimeout(1500)      │  (user sees msg)     │  (clean exit)
│                        │                      │
└────────────────────────┴──────────────────────┴──────────→
                                                    Time

Result: User sees success message, then smoothly returns home
```

---

## State Management

```
┌─────────────────────────────────────────────────────────────┐
│                   STATE LIFECYCLE                            │
└─────────────────────────────────────────────────────────────┘

BEFORE FIX:
┌──────────────┐
│ Initial State│ ─→ User selects meals
└──────────────┘
       ↓
┌──────────────┐
│ Dirty State  │ ─→ User saves
└──────────────┘
       ↓
┌──────────────┐
│ Saved State  │ ─→ Back button pressed
└──────────────┘
       ↓
┌──────────────┐
│ Dirty State  │ ❌ OLD STATE VISIBLE!
└──────────────┘


AFTER FIX:
┌──────────────┐
│ Initial State│ ─→ User selects meals
└──────────────┘
       ↓
┌──────────────┐
│ Dirty State  │ ─→ User saves
└──────────────┘
       ↓
┌──────────────┐
│ Redirect Home│ ✅ CLEAN EXIT!
└──────────────┘
       ↓
┌──────────────┐
│ Home Page    │ ─→ No old state
└──────────────┘
```

---

## Browser History Comparison

```
┌─────────────────────────────────────────────────────────────┐
│              HISTORY STACK COMPARISON                        │
└─────────────────────────────────────────────────────────────┘

BEFORE (navigate with push):
┌─────────────────────┐
│  Selection (saved)  │ ← Current (can go back)
├─────────────────────┤
│  Selection (dirty)  │ ← Can go back here ❌
├─────────────────────┤
│  Selection (init)   │ ← Can go back here ❌
├─────────────────────┤
│  Home               │
└─────────────────────┘
Total entries: 4


AFTER (navigate with replace):
┌─────────────────────┐
│  Home               │ ← Current (nowhere to go back)
└─────────────────────┘
Total entries: 1

Clean history = Better UX!
```

---

## Event Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                 COMPLETE EVENT FLOW                          │
└─────────────────────────────────────────────────────────────┘

┌─────────────┐
│  Component  │
│   Mounts    │
└──────┬──────┘
       │
       ├─→ replaceState() ─→ Replace history entry
       │
       ├─→ addEventListener() ─→ Listen for back button
       │
       └─→ Render UI
              │
              ├─→ User interacts
              │      │
              │      ├─→ Select meals
              │      │
              │      └─→ Click Save
              │             │
              │             ├─→ saveSelections()
              │             │      │
              │             │      ├─→ Save to DB
              │             │      │
              │             │      ├─→ toast.success()
              │             │      │
              │             │      └─→ setTimeout(1500)
              │             │             │
              │             │             └─→ navigate('/', {replace})
              │             │                    │
              │             │                    └─→ Redirect to Home
              │             │
              │             └─→ OR User presses Back
              │                    │
              │                    ├─→ popstate event
              │                    │
              │                    ├─→ handlePopState()
              │                    │
              │                    └─→ navigate('/', {replace})
              │                           │
              │                           └─→ Redirect to Home
              │
              └─→ Component Unmounts
                     │
                     └─→ removeEventListener() ─→ Cleanup
```

---

## Key Concepts

### 1. History API Methods
```
┌──────────────────────────────────────┐
│  pushState()    │ Adds new entry     │
│  replaceState() │ Replaces current   │ ✅ We use this
│  back()         │ Go back one entry  │
│  forward()      │ Go forward         │
└──────────────────────────────────────┘
```

### 2. Navigation Options
```
┌──────────────────────────────────────┐
│  navigate('/path')                   │ Adds to history
│  navigate('/path', {replace: true})  │ Replaces ✅
└──────────────────────────────────────┘
```

### 3. Event Listeners
```
┌──────────────────────────────────────┐
│  popstate    │ Back/Forward pressed  │ ✅ We listen
│  hashchange  │ URL hash changed      │
│  beforeunload│ Page about to close   │
└──────────────────────────────────────┘
```

---

**Visual Summary:**
- ❌ Before: Multiple history entries, old states visible
- ✅ After: Single history entry, clean navigation
- 🎯 Result: Professional, native app-like behavior
